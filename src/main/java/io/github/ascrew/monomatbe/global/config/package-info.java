/**
 * 애플리케이션 전역 설정 패키지.
 *
 * <p>Redis, WebSocket, Spring Security 등 인프라 컴포넌트의
 * Bean 등록 및 설정을 담당합니다.
 * 환경(dev/prod)에 따라 분리된 설정은 하위 패키지에서 관리합니다.
 */
package io.github.ascrew.monomatbe.global.config;