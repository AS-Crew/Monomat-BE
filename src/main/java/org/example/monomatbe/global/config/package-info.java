/**
 * Config 패키지
 *
 * Spring Framework 및 제3자 라이브러리 설정을 포함합니다.
 *
 * 포함 사항:
 * - @Configuration 클래스
 * - SecurityConfig (Spring Security)
 * - WebConfig (CORS, 인터셉터 등)
 * - JpaConfig (JPA/Hibernate 설정)
 * - RedisConfig (Redis 연동 설정)
 * - SwaggerConfig (API 문서화)
 */
package org.example.monomatbe.global.config;

